/**
 * Returns true if specified platform has stable viewport. Stable means not changing from time to
 * time.
 * @param platform - platform identifier.
 */ export function isStableViewportPlatform(platform) {
  return [
    'macos',
    'tdesktop',
    'unigram',
    'web',
    'weba'
  ].includes(platform);
}
//# sourceMappingURL=isStableViewportPlatform.js.map