import { keyToExternal } from "./keys.js";
/**
 * Serializes theme parameters to representation sent from the Telegram application.
 */ export function serializeThemeParams(themeParams) {
  return JSON.stringify(Object.entries(themeParams).reduce((acc, [key, value])=>{
    if (value) {
      acc[keyToExternal(key)] = value;
    }
    return acc;
  }, {}));
}
//# sourceMappingURL=serializeThemeParams.js.map