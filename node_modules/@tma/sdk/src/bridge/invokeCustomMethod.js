import { request } from "./request.js";
export async function invokeCustomMethod(method, params, requestId, options = {}) {
  const { result, error } = await request('web_app_invoke_custom_method', {
    method,
    params,
    req_id: requestId
  }, 'custom_method_invoked', options);
  if (error) {
    throw new Error(error);
  }
  return result;
}
//# sourceMappingURL=invokeCustomMethod.js.map