import { isRecord } from "../../misc/isRecord.js";
/**
 * Returns true in case, passed value contains path `TelegramWebviewProxy.postEvent` property and
 * `postEvent` is a function.
 * @param value - value to check.
 */ export function hasWebviewProxy(value) {
  return 'TelegramWebviewProxy' in value && isRecord(value.TelegramWebviewProxy) && 'postEvent' in value.TelegramWebviewProxy && typeof value.TelegramWebviewProxy.postEvent === 'function';
}
//# sourceMappingURL=hasWebviewProxy.js.map